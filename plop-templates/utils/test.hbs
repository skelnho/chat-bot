import { describe, it, expect } from 'vitest'
import { {{camelCase name}} } from './{{kebabCase name}}'

describe('{{camelCase name}}', () => {
  it('UPDATE', () => {
    const input = 'foo'
    const output = 'foo'
    expect({{camelCase name}}(input)).toBe(output)
  })
})
